###########################################################
###################### General Setup ######################
###########################################################

### Paths #################################################

# 定义应用代码在宿主机路径
APP_CODE_PATH_HOST=../newphp
API_CODE_PATH_HOST=../api
MEMBER_CODE_PATH_HOST=../member
PORTAL_CODE_PATH_HOST=../portal
CONSULT_CODE_PATH_HOST=../consult

# 定义应用代码在容器中的路径(`:cached`, `:delegated`; Docker Sync: `:nocopy`)
APP_CODE_PATH_CONTAINER=/var/www:cached:nocopy
API_CODE_PATH_CONTAINER=/usr/src/app:cached:nocopy
MEMBER_CODE_PATH_CONTAINER=/usr/src/app:cached:nocopy
PORTAL_CODE_PATH_CONTAINER=/usr/src/app:cached:nocopy
CONSULT_CODE_PATH_CONTAINER=/usr/src/app:cached:nocopy

# Choose storage path on your machine. For all storage systems
DATA_PATH_HOST=./data

### Drivers ################################################

# All volumes driver
VOLUMES_DRIVER=local

# All Networks driver
NETWORKS_DRIVER=bridge

### Docker compose files ##################################

# Select which docker-compose files to include. If using docker-sync append `:docker-compose.sync.yml` at the end
COMPOSE_FILE=docker-compose.yml:docker-compose.sync.yml

# 在Windows下将冒号改成分号
COMPOSE_PATH_SEPARATOR=:

### PHP Version ###########################################

# 指定php版本(Workspace和PHP-FPM容器), (7.2 - 7.1 - 7.0 - 5.6 - 5.5)
PHP_VERSION=7.1

### PHP Interpreter #######################################

PHP_INTERPRETER=php-fpm

### Docker Host IP ########################################

# Enter your Docker Host IP (will be appended to /etc/hosts). Default is `10.0.75.1`
DOCKER_HOST_IP=10.0.75.1

### Remote Interpreter ####################################

# Choose a Remote Interpreter entry matching name. Default is `laradock`
PHP_IDE_CONFIG=serverName=parallel

### Windows Path ##########################################

# A fix for Windows users, to ensure the application path works
COMPOSE_CONVERT_WINDOWS_PATHS=1

### Environment ###########################################

# If you need to change the sources (i.e. to China), set CHANGE_SOURCE to true
CHANGE_SOURCE=true

### Docker Sync ###########################################

# If you are using Docker Sync. For `osx` use 'native_osx', for `windows` use 'unison', for `linux` docker-sync is not required
DOCKER_SYNC_STRATEGY=native_osx

###########################################################
################ Containers Customization #################
###########################################################

## NGINX #################################################

NGINX_HOST_HTTP_PORT=80
NGINX_HOST_HTTPS_PORT=443
NGINX_HOST_LOG_PATH=./logs/nginx/
NGINX_SITES_PATH=./nginx/sites/
NGINX_PHP_UPSTREAM_CONTAINER=php-fpm
NGINX_PHP_UPSTREAM_PORT=9000

## MYSQL #################################################

MYSQL_VERSION=latest
MYSQL_DATABASE=parallel
MYSQL_USER=root
MYSQL_PASSWORD=yfyunmysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=yfyunmysql
MYSQL_ENTRYPOINT_INITDB=./mysql/docker-entrypoint-initdb.d

### REDIS #################################################

REDIS_PORT=6379

## ELASTICSEARCH #########################################

ELASTICSEARCH_HOST_HTTP_PORT=9200
ELASTICSEARCH_HOST_TRANSPORT_PORT=9300

# API #########################################
API_PORT=8085

# MEMBER #########################################
MEMBER_PORT=1337

# PORTAL #########################################
PORTAL_PORT=1338

# CONSULT #########################################
CONSULT_PORT=8088
